I'll be getting a data. So here it is saying that it is not available because first we'll have to upload the data. We are not going to upload the data guys. In that case you will get the same error. So please make sure that you are going to upload a data over here. Or else you can read it from the GitHub. You can read it from the drive. Anywhere you can keep this data and you can read it. Now what I'm doing guys, so here let's try to read the data now. So see I got the PDF text. I can show you how the text looks like.This is the text, guys. The complete text which I am able to get from the PDF itself. Now after that, what I will do, I will divide the text into chunks. Now this divide the text into the chunk, it is a hyper parameter, okay? So this logic basically, what we can do, this logic, we can decide how we are going to be divide our data into the chunks, okay, and what will be the size of the chunk, right? Now I can give you a couple of examples, so I kept for all of you. Now first of all guys, see, whenever we are going to see.We are going to load the data. Okay, so this is the complete step which we need to follow. As of now, my data was not too much, but if we are going to train our model from scratch, okay, or if we are going to do the non-instructional fine tuning, so you need to follow the same step. So first you need to collect our data, which we have already done. So see, this is my data. Now after collecting the data, guys, what we have to do, we have to cleaning the cleaning and filtering the data. Then we have to divide the data into this chunking, okay.Multiple chunks because we cannot give this entire data at a single go, right? To train my model, then tokenization means we have to convert our data into the specific numerical ID. Okay, after the chunking.So to every token into the given chunk, one ID, one numerical ID will be assigned, and then we are going to perform the training, which is a next token prediction. Right, so this entire step we have to follow. Now guys, here I kept one more thing, one more table. So this table could be very much important. So just try to read this particular table carefully. So here I given the model name, max to context window, approx what, how many words could be there, and the comment. Right, so GPT one model, so it was able to handle five.One two token and there was approx three hundred and fifty watts. Now GPT two one thousand token, okay, around seven hundred and fifty watts. GPT three two thousand token, one five kw around. GPT three five four hundred and nine token, around three kw. And if you are talking about GPT four four zero, so it is follow, it is able to handle up to one lakh twenty eight thousand token, around one lakh watts. Okay, now over here guys, you can see. So whenever we are going to chunk the data, okay, so we have to keep something in our mind, right? So our model.How much basically token can handle, and whenever we are going to be divide the data into the chunks. Okay, so we can follow some regex pattern also means we can divide our data into the paragraph, we can divide our data into the segment. Okay, or a semantic chunk and all, and then basically we can or else basically we can go ahead with the token length also. So let's say if we are going to be training GPT four kind of model, so let's say we are going to be load the data, this PDF data. Okay, so we can give.The bigger bigger chunk to the GPT four model to retrain it to the fine tuning it okay, and similar similar at the time of the unsupervised tuning as well. Okay, means the first step, the initial stage. Okay, now here guys, you can see so this chunking we can do based on what, based on this particular pattern. See, here I written based on the paragraph semantic chunking, right? We can write some regex rule and all, and then we can or else we can go ahead with the token length, means how many tokens we can be in the single chunk according to that also we can.Or else we can take the both strategy. Okay, so I have written a function over here. Now see the function. So it's split paragraph. So first we'll take the pages. Okay, this data. Now we are going to be split based on the line. Okay, based on the new long line means the double line. Here you can see in the PDF itself. So if you will look into the PDF, so where it went here. So here you can see between this line and this line we have major gap, right? So we are going to be divide our data based on this major line. Okay, soIs the regex pattern? You can write any regex pattern. You can read out the ChatGPT model research paper, any other research paper, right? How they have done the unsupervised pre-training, how they have divided the data into the chunks and tag text and all, right? So it will also help you. Okay, but this is also a generic practice. Then what I'm doing, I'm iterating on top of the chunk, and if I'm saying over here, if there is a 30 character, more than 30 character, then only I'm going to be append the chunk into this particular list, okay? So whatever like.Whatever pages we have is on we are iterating on top of the data. Okay, and on the entire list, then we are chunking based on this strategy. If inside this chunk we have more than thirty character, more than thirty character means what ten to twelve token. Okay, or eight to nine token. So more than thirty character only, then only we are going to append inside this particular paragraph. Okay, if I will run it guys, now here you will see we are going to be uh, split our data. So here uh, inside this particular variable, I'll get my data split data. Now see, so uh, see what I have, I have the.First chunk, then second chunk. Okay, then third chunk. Then here you can see the fourth chunk. But guys, the data should not be like this. The data should be available into this particular form. Okay, we should have this. Okay, we should have this text column and under this text column the chunk should be present. So what I will do now I will map my data okay to the text data. So here see my data guys. We have this text okay data. This list itself said. Okay, okay, and here number of rows is four.So if you will look into the pre-built data guys, this was my pre-built data. If you will look into my pre-built data, so this was a pre-built data which I loaded from the hiking place. In the same way, we are able to convert our data. See here is my data. So now you can check the first row, second row, whatever row you can check. So let's say if I'm going to be check the first row, okay, I will simply put the zero. So here you will be able to get the first row. Likewise, you can get the second row, okay. So in total we have four throw. So guys, in whatever way I.Having my pre-built data, the data was already available over the Hadoop space. Now I'm able to convert my dataset into the same format, okay, my custom dataset. So this all the technique, all the pre-processing step you will have to follow with respect to your plain text. Now.To the next part. So the next thing is basically we have to select the model. So first of all let me create the multiple cells, okay. Now here we'll have to select the model. So let me make a heading. So the heading is let's select the model. So guys, model wise what I'm doing, I'm going to be select a very tiny model because I cannot bear the expensive, the large model inside with this particular GPU, with this particular memory. We require more VRAM and all. So if you have Google Colab Pro access or.You have more VRAM, then you can try out with this particular model. I'm using this model, Tiny, from the TinyLlama repository. So TinyLlama 1.1B intermediate step 1431k 3T. Okay, now what does it mean? What is the meaning of this line? So I written a meaning of this line over here, see. So the checkpoint is from the TinyLlama model, okay, which trained up to 1.54 million step over 300, sorry, 3 million to 3 trillion tokens captured.Before the final conversion. So guys, this particular model is a tiny llama model. This model trained till this particular step, okay, around 14 lakh step, okay, on top of this much of token. So I'm using this model to train. I'm using this as a base model. This is my base model, guys. Okay, this model has not been trained on any instruction data or not on any domain specific data. This is my base model, simple pre-trained model. Okay, so I'm going to use this particular model to.The domain specific fine tuning on top of my PDF data, right? I already prepared data. Now what I will do guys, I will basically perform the fine tuning. So first of all guys, what I will do, I will load the, I will load this specific library like AutoTokenizer, AutoLM Trainer, training arguments and collector for language model. I think this will not be required, or training till training arguments.And the library. Now here, what I will do, guys, I will load the tokenizer. Okay, I will tokenize my data because I already performed the chunking. Now I will perform the tokenization using this particular tokenizer. Okay, so I am loading a tokenizer. Now after the, I have to execute one more line of code. So if padding is none, then in that case I am going to be assigning this end of token to the padding. Right, now why it is required? Let me give you the reason of that as well. So here I kept one image with that you can easily understand.What is a pad token? So pad token we use to make all sequences in a batch to the same length. So let's say in input word we have two token, hello world. In input three, input two we have three token, good morning everyone. Technically there is a three token, hello world this full stop, then good morning everyone full stop. Now here you can see the size of the token, size of the input one and input two is different. So if I want to make it same, so here what I'll do, I will do the padding. Okay. Now how I am going to do the.So at the end, okay, at the end of this particular, see here the length of this particular input is three, sorry, four. Okay, one, two, three, four. Now what I will do? So here at the end of the sentence, hello world. Okay, after this particular world, I will add one more token. Okay, that's going to be us. End of the token. Okay, this us. So if I am going to be appending the us token, now how my sentence will look like? Hello world us. Okay, so the length of this input one and input two is going to be same. So that's a simple use of this line.And here we are able to append this end of the token. Okay, now after that, what I will do, guys? So after that case, we are going to be pre-process the data. Now the pre-processing step is pretty simple. So here we are passing an example, means every text chunk. Now after taking every text chunk, see we are truncating, okay, we are padding and we are like defining the max length. So what is the meaning of this particular parameter? I'll tell you. Now here we have one very important line. So to token, okay, to the label actually.To be assigned this input item, okay, and then we are going to return the token. So let me execute it and let me explain the meaning of this each and every line because it is very much important.Here what I'm doing, guys. I'm going to be, I'm going to be like add some example and all, and with this particular example, we'll be able to understand. Now see, so whenever we are passing the text, so let's assume this is my text. Okay, metformin improves insulin sensitivity in the liver. So what will happen? So this tokenizer token. Okay, it is going to be take this particular text. Now truncation true, what it will do? So if the text is longer than five, twelve tokens, it is going to be cut off that. Padding true means if the text is shorter.Token it's going to be bad okay and the fixed length of this of this particular sentence of this like whenever we are doing the tokenization it will be five twelve okay it's going to be less and more to this five turn then we are either we are going to be truncate or we are going to be perform the padding so after processing it I will be getting this particular input so input I will be getting this particular output where I will be having two column input IDs okay which is going to be assigned to every token and then attention mask attention.Which token is important for me to the attention layer? Then label guys, so token label, the model should try to predict the next token. We took this plain text, we convert into the text, we have to predict the next word. Okay, and for that, what we have proved in Zulip, if computing, we'll have to.And here remove column, check it. You can simply check it. See here is what here. So model loading might take some time because the model size is around four GB. Okay, so in your, it depends on your internet speed. In my case, it might take like four to five minute.As I told you, in such a trading argument, so guys, we have so many trading arguments inside this class. If you will see the list, you will get the hundreds of argument. So how you can get the detail of it? So for that you can simply call this help trading argument, okay.Already loaded this class from the transformer.This class from the transformer. Now simply pass it to the help method. Okay. Now once you will run it, see here you will get the detail of each and every parameter, whatever is there.I mentioned the required parameter only, so I kept the meaning of this particular parameter. Let me mention it over here.Output directory means where we are going to save the model. Overwrite output directory means we are going to overwrite the previous model if we are saving the new model, it will overwrite to the previous model. Number of training reports, how many epochs we are, let's say will go spend second, second is data. Okay, the class, so you can pause the video because guys, you have you haven't focused because we are training the entire model, we are retraining the entire model over here. If you will check C, we are loading a model, we are not going to be select any, we are not going to be select any basically last row, okay, or the initial row, we are not going to do any LoRA and QLoRA, we are simply retraining the weight of the entire model. And my machine is not capable to do that thing right now, and because of that I am getting the, I am getting this out of memory error. So what is the solution of it? So the solution of it is LoRA, okay, parameter efficient fine tuning. Now before the LoRA guys, let me show you the other method also. So here I return some sort of a description. So let me keep the description over here.So what is the description, guys? So here we have, we are not specifying anything, means this is the full fine tuning. Now we have to two method for the partial fine tuning. The first, freeze some layer and fine tune unfreeze layer, which is the old CNN method or bird style method, right? The second is LoRA, append some external weight to the already trained pre-trained weight. So guys, I will show you this both method, okay, I will more focus on the LoRA. I am not going to explain this freeze some layer and all. I will give you the code, I will keep it over here, but I am not.Because I already explained in my previous video. Come back to the new pill, then we are unfreezing the last four PPO, so a variable basically, get my data. Okay, again I.i̇pLowest is more eight nanometer, but so to be my quantize Laura model, it I think I ran because I already loaded the model, the same one, that you guys I will, beginning so that is that so you, can see here.
